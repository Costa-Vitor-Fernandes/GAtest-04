name: 2Conventional Commits Validation

on:
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  validate-commits:
    name: Validate Conventional Commits
    runs-on: ubuntu-latest
    
    steps:
      - name: 
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
        run: |
          npm install --save-dev \
            @commitlint/cli@18.4.3 \
            @commitlint/config-conventional@18.4.3 \
            conventional-changelog-conventionalcommits@7.0.2 \
            conventional-recommended-bump@9.0.0 \
            semver@7.5.4

      - name: Validate PR title
        run: |
          PR_TITLE="${{ github.event.pull_request.title }}"          
          if ! echo "$PR_TITLE" | npx commitlint --verbose --config scripts/ci/commitlint.config.js; then
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "❌ Título do PR está fora do padrão!"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            exit 1
          fi

      - name: Validate PR commit messages
        env:
          GITHUB_BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: |
          node ./scripts/ci/validate-commits.cjs

      - name: Calculate next version
        id: version
        env:
          GITHUB_BASE_REF: ${{ github.event.pull_request.base.ref }}
        run: |
          node ./scripts/ci/calculate-version.cjs
          
      - name: Create or Update Comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          edit-mode: replace
          comment-author: 'github-actions[bot]'
          body-includes: '🤖 This comment is automatically updated.'
          body: |
            ## ${{ steps.version.outputs.release_type == 'major' && '💥' || (steps.version.outputs.release_type == 'minor' && '✨' || '🐛') }} Version Impact Analysis

            **Current Version:** `v${{ steps.version.outputs.current }}`
            **Predicted Version:** `v${{ steps.version.outputs.next }}`
            **Release Type:** **${{ steps.version.outputs.release_type }}**

            ${{ steps.version.outputs.breaking == 'true' ? '> ⚠️ **WARNING:** This PR contains BREAKING CHANGES!' : '' }}

            🤖 This comment is automatically updated.